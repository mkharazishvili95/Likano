<!DOCTYPE html>
<html lang="ka">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Likano</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="icon" type="image/png" sizes="32x25" href="https://ik.imagekit.io/rqxdk712q/products/92efa6d7-3b27-42ec-bfab-212e240b64d4_fav" />
    <link href="https://fonts.googleapis.com/css2?family=BPG+Arial:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #2563eb;
            --primary-hover: #1d4ed8;
            --primary-soft: #eff6ff;
            --dark: #0f172a;
            --slate: #64748b;
            --bg: #f8fafc;
            --border-color: #e2e8f0;
            --card-shadow: 0 4px 20px -2px rgba(15,23,42,.05);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: 'BPG Arial', sans-serif;
        }

        header {
            background: var(--bg);
            border-bottom: 1px solid var(--border-color);
        }

        .container-header {
            max-width: 1380px;
            margin: 0 auto;
            padding: 16px 32px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .branding-group {
            display: flex;
            align-items: center;
            gap: 18px;
        }

        .logo {
            display: flex;
            align-items: center;
            text-decoration: none;
        }

            .logo img {
                height: 96px;
                max-height: 96px;
                width: auto;
                transition: transform .25s ease;
            }

            .logo:hover img {
                transform: scale(1.05) rotate(-2deg);
            }

        .contact-group {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .contact-btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 10px 16px;
            background: #fff;
            border: 1px solid var(--border-color);
            border-radius: 10px;
            color: var(--dark);
            text-decoration: none;
            font-weight: 500;
            font-size: 1.05rem;
            transition: all 0.25s ease;
            white-space: nowrap;
        }

            .contact-btn:hover {
                background: var(--primary-soft);
                color: var(--primary);
                border-color: var(--primary);
                transform: translateY(-2px);
                box-shadow: 0 6px 16px rgba(37,99,235,0.12);
            }

            .contact-btn i {
                font-size: 1.3rem;
            }

            .contact-btn.phone i {
                color: #10b981;
            }

            .contact-btn.map i {
                color: #ef4444;
            }

        .slogan {
            font-size: 1.18rem;
            font-weight: 700;
            color: #2563eb;
            background: #fff;
            border-radius: 16px;
            padding: 16px 36px;
            margin: 0 20px;
            letter-spacing: 0.3px;
            text-align: center;
            box-shadow: 0 4px 24px rgba(37,99,235,0.10);
            border: 1px solid #e2e8f0;
            transition: background 0.2s, color 0.2s;
        }

        main {
            max-width: 1380px;
            margin: 32px auto 48px;
            padding: 0 24px;
        }

        footer {
            text-align: center;
            padding: 40px 0 20px;
            font-size: 0.97rem;
            color: var(--slate);
            background: var(--bg);
        }

            footer a {
                color: var(--primary);
                text-decoration: none;
            }

        @@media (max-width: 991px) {
            .container-header

        {
            flex-direction: column;
            gap: 20px;
            padding: 16px 20px;
        }

        .logo img {
            height: 80px;
        }

        .contact-group {
            flex-direction: column;
            width: 100%;
        }

        .contact-btn {
            justify-content: center;
            width: 100%;
        }

        .slogan {
            margin: 12px 0 0 0;
            font-size: 1.06rem;
            text-align: center;
            justify-content: center;
            height: auto;
            border-left: none;
            border-top: 3px solid var(--primary);
            border-radius: 0 0 6px 6px;
            background: #f3f7fd;
        }

        .branding-group {
            flex-direction: column;
            align-items: flex-start;
            gap: 8px;
            width: 100%;
        }

        }

        @@media (max-width: 576px) {
            .logo img

        {
            height: 68px;
        }

        .contact-btn {
            font-size: 0.98rem;
            padding: 9px 14px;
        }

        }
    </style>
    @RenderSection("Styles", required: false)
</head>
<body>
    <header>
        <div class="container-header">
            <div class="branding-group">
                <a class="logo" href="/" title="Likano.Ge - სამზარეულოს ტექნიკა">
                    <img src="https://ik.imagekit.io/rqxdk712q/products/17827bc1-7332-4122-b0aa-7e5adc3e229e_likanologo"
                         alt="Likano Logo" />
                </a>
                <div class="slogan">
                    სამზარეულო იწყება სწორი ტექნიკით
                </div>
            </div>
            <div class="contact-group">
                <a href="tel:+995599457950" class="contact-btn phone" title="დაკავშირება">
                    <i class="fas fa-phone-alt"></i>
                    599 45 79 50
                </a>
                <a href="/contact" class="contact-btn map" title="სრული მისამართი და რუკა">
                    <i class="fas fa-map-marker-alt"></i>
                    თბილისი, აგლაძის ქ. №15
                </a>
            </div>
        </div>
    </header>

    <main role="main">
        @RenderBody()
    </main>

    <footer>
        © @DateTime.Now.Year Likano.Ge — ყველა უფლება დაცულია.
        Developed by
        <a href="https://ge.linkedin.com/in/mikheil-kharazishvili-b179a5269"
           target="_blank">
            Mikheil Kharazishvili
        </a>
    </footer>

    @RenderSection("Scripts", required: false)
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>