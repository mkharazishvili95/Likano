@using Likano.Domain.Enums
@using Likano.Infrastructure.Queries.Product.Models.Similar
@model Likano.Infrastructure.Queries.Product.Models.Details.GetProductDetailsResponse

@{
    Layout = "_Layout";
    ViewData["Title"] = Model.Title ?? "პროდუქტის დეტალები";
}
@{
    Func<ProductType, string> getGeorgianType = type => type switch
    {
        ProductType.BuiltIn => "ჩასაშენებელი",
        ProductType.Freestanding => "დასადგამი",
        ProductType.WallMounted => "კედელზე დასაკიდი",
        ProductType.CounterTop => "ზედაპირზე / მაგიდაზე დასადგამი",
        ProductType.FloorMounted => "იატაკზე დასადგამი",
        _ => ""
    };
}

<style>
    :root {
        --primary: #6366f1;
        --primary-dark: #4f46e5;
        --primary-light: #818cf8;
        --secondary: #ec4899;
        --success: #10b981;
        --danger: #ef4444;
        --surface: #ffffff;
        --surface-alt: #f8fafc;
        --border: #e2e8f0;
        --text-primary: #0f172a;
        --text-secondary: #64748b;
        --shadow-sm: 0 1px 3px rgba(0,0,0,0.05);
        --shadow-md: 0 4px 12px rgba(0,0,0,0.08);
        --shadow-lg: 0 20px 40px rgba(0,0,0,0.12);
        --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .zoom-lens {
        position: absolute;
        border: 2px solid #6366f1;
        border-radius: 50%;
        width: 120px;
        height: 120px;
        pointer-events: none;
        opacity: 0.85;
        background-repeat: no-repeat;
        background-size: 200% 200%;
        z-index: 100;
        display: none;
    }
    
    .product-page-wrapper {
        min-height: 80vh;
        background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
        padding: 2.5rem 0 4rem 0;
    }

    .product-container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 0 1.5rem;
    }

    .product-meta-bar {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 16px;
        padding: 1rem 1.5rem;
        margin-bottom: 2rem;
        box-shadow: var(--shadow-sm);
        border: 1px solid var(--border);
        display: flex;
        gap: 2rem;
        align-items: center;
        justify-content: flex-end;
        flex-wrap: wrap;
    }

    .meta-item {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--text-secondary);
        font-size: 0.95rem;
        font-weight: 500;
    }

        .meta-item svg {
            width: 18px;
            height: 18px;
            fill: var(--primary);
            opacity: 0.8;
        }

    .meta-sep {
        color: var(--border);
        font-weight: 300;
    }

    .product-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 2rem;
        align-items: start;
    }

    .gallery-card {
        background: var(--surface);
        border-radius: 24px;
        padding: 2.5rem;
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--border);
        position: sticky;
        top: 20px;
    }

    .product-title {
        font-size: 2rem;
        font-weight: 800;
        color: var(--text-primary);
        margin-bottom: 1rem;
        line-height: 1.2;
        background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        padding-bottom: 0.2em;
        box-decoration-break: clone;
        -webkit-box-decoration-break: clone;
    }

    .product-code {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        background: linear-gradient(135deg, #ede9fe 0%, #fce7f3 100%);
        color: var(--primary-dark);
        padding: 0.5rem 1.25rem;
        border-radius: 50px;
        font-size: 0.9rem;
        font-weight: 600;
        margin-bottom: 2rem;
    }

    .main-image-wrapper {
        width: 100%;
        aspect-ratio: 1;
        border-radius: 20px;
        overflow: hidden;
        background: var(--surface-alt);
        position: relative;
        margin-bottom: 1.5rem;
        box-shadow: var(--shadow-md);
        border: 1px solid var(--border);
    }

    .main-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        cursor: zoom-in;
        transition: var(--transition);
    }

        .main-image:hover {
            transform: scale(1.05);
        }

    .thumbnail-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(80px, 1fr));
        gap: 0.75rem;
    }

    .thumbnail {
        aspect-ratio: 1;
        border-radius: 12px;
        overflow: hidden;
        cursor: pointer;
        border: 3px solid transparent;
        transition: var(--transition);
        box-shadow: var(--shadow-sm);
        background: var(--surface-alt);
    }

        .thumbnail:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
            border-color: var(--primary-light);
        }

        .thumbnail.active {
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(99, 102,241, 0.15);
        }

        .thumbnail img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

    .info-card {
        background: var(--surface);
        border-radius: 24px;
        padding: 2.5rem;
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--border);
        display: flex;
        flex-direction: column;
        gap: 2rem;
    }

    .price-availability {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1.5rem;
        padding-bottom: 2rem;
        border-bottom: 2px solid var(--border);
        flex-wrap: wrap;
    }

    .product-price {
        font-size: 1.7rem;
        font-weight: 900;
        background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        letter-spacing: -0.02em;
    }

    .availability-badge {
        padding: 0.75rem 1.5rem;
        border-radius: 50px;
        font-weight: 600;
        font-size: 0.95rem;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        box-shadow: var(--shadow-sm);
    }

        .availability-badge.in-stock {
            background: linear-gradient(135deg, #d1fae5 0%, #a7f3d0 100%);
            color: #065f46;
        }

        .availability-badge.out-of-stock {
            background: linear-gradient(135deg, #fee2e2 0%, #fecaca 100%);
            color: #991b1b;
        }

        .availability-badge::before {
            content: '';
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: currentColor;
            animation: pulse 2s ease-in-out infinite;
        }

    @@keyframes pulse {
        0%, 100%

    {
        opacity: 1;
    }

    50% {
        opacity: 0.5;
    }

    }

    .specs-section {
        display: flex;
        flex-direction: column;
        gap: 1.25rem;
    }

    .section-title {
        font-size: 1.25rem;
        font-weight: 700;
        color: var(--text-primary);
        display: flex;
        align-items: center;
        gap: 0.75rem;
        margin-bottom: 0.5rem;
    }

    .section-title::before {
        content: '';
        display: none;
    }
    .section-title .icon {
        display: inline-flex;
        align-items: center;
        margin-right: 0.5rem;
    }

        .section-title::before {
            content: '';
            width: 4px;
            height: 24px;
            background: linear-gradient(180deg, var(--primary) 0%, var(--secondary) 100%);
            border-radius: 4px;
        }

    .specs-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 1rem;
    }

    .spec-item {
        background: var(--surface-alt);
        padding: 1.25rem;
        border-radius: 12px;
        border: 1px solid var(--border);
        transition: var(--transition);
    }

        .spec-item:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
            border-color: var(--primary-light);
            background: var(--surface);
        }

    .spec-label {
        font-size: 0.75rem;
        color: var(--text-secondary);
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-bottom: 0.5rem;
    }

    .spec-value {
        font-size: 1.05rem;
        color: var(--text-primary);
        font-weight: 600;
    }

    .actions-section {
        padding-top: 2rem;
        border-top: 2px solid var(--border);
    }

    .btn-back {
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        padding: 1rem 2rem;
        border-radius: 12px;
        font-weight: 600;
        font-size: 1rem;
        border: 2px solid var(--primary);
        background: var(--surface);
        color: var(--primary);
        text-decoration: none;
        transition: var(--transition);
        box-shadow: var(--shadow-sm);
    }

        .btn-back:hover {
            background: var(--primary);
            color: white;
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }

    .specs-section-upgraded {
        margin-top: 2.5rem;
        padding: 2.5rem 2rem;
        border-radius: 28px;
        background: rgba(255,255,255,0.7);
        box-shadow: 0 8px 32px 0 rgba(99,102,241,0.10), 0 1.5px 8px 0 rgba(236,72,153,0.08);
        backdrop-filter: blur(12px);
        border: 1.5px solid #e0e7ff;
        position: relative;
    }

    .specs-header {
        display: flex;
        align-items: center;
        margin-bottom: 2rem;
        gap: 1rem;
    }

    .specs-accent {
        width: 6px;
        height: 38px;
        border-radius: 6px;
        background: linear-gradient(180deg, #6366f1 0%, #ec4899 100%);
        box-shadow: 0 2px 8px #6366f133;
    }

    .specs-title {
        font-size: 1.45rem;
        font-weight: 900;
        color: #6366f1;
        letter-spacing: -0.5px;
        display: flex;
        align-items: center;
        gap: 0.7rem;
        background: linear-gradient(90deg, #6366f1 0%, #ec4899 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .specs-grid-upgraded {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 1.5rem;
    }

    .spec-card {
        display: flex;
        align-items: flex-start;
        gap: 1.1rem;
        background: rgba(243,244,246,0.85);
        border-radius: 18px;
        padding: 1.25rem 1.1rem;
        box-shadow: 0 2px 12px 0 rgba(99,102,241,0.07);
        border: 1.5px solid #e0e7ff;
        transition: box-shadow 0.2s, transform 0.2s, border 0.2s;
        min-height: 80px;
        position: relative;
    }

        .spec-card:hover {
            box-shadow: 0 6px 24px 0 rgba(99,102,241,0.13);
            border: 1.5px solid #6366f1;
            transform: translateY(-2px) scale(1.03);
            background: rgba(255,255,255,0.95);
        }

    .spec-icon {
        font-size: 1.6rem;
        color: #6366f1;
        background: linear-gradient(135deg, #ede9fe 0%, #fce7f3 100%);
        border-radius: 10px;
        padding: 0.5rem 0.7rem;
        display: flex;
        align-items: center;
        justify-content: center;
        min-width: 38px;
        min-height: 38px;
        box-shadow: 0 1px 6px #6366f122;
    }

    .spec-label-upgraded {
        font-size: 0.82rem;
        color: #64748b;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-bottom: 0.2rem;
    }

    .spec-value-upgraded {
        font-size: 1.13rem;
        color: #0f172a;
        font-weight: 700;
        letter-spacing: -0.01em;
        word-break: break-word;
    }

    .actions-section-upgraded {
        margin-top: 2.5rem;
        display: flex;
        justify-content: flex-end;
    }

    .btn-back-upgraded {
        display: inline-flex;
        align-items: center;
        gap: 0.8rem;
        padding: 1rem 2.2rem;
        border-radius: 16px;
        font-weight: 800;
        font-size: 1.08rem;
        border: none;
        background: linear-gradient(90deg, #6366f1 0%, #ec4899 100%);
        color: #fff;
        text-decoration: none;
        box-shadow: 0 4px 18px 0 rgba(99,102,241,0.13);
        transition: background 0.2s, box-shadow 0.2s, transform 0.2s;
        cursor: pointer;
        letter-spacing: -0.5px;
        position: relative;
        overflow: hidden;
    }

        .btn-back-upgraded svg {
            margin-right: 0.2rem;
            fill: #fff;
            transition: transform 0.2s;
        }

        .btn-back-upgraded:hover {
            background: linear-gradient(90deg, #4f46e5 0%, #ec4899 100%);
            box-shadow: 0 8px 32px 0 rgba(99,102,241,0.18);
            transform: translateY(-2px) scale(1.04);
        }

        .btn-back-upgraded:active {
            transform: scale(0.98);
        }

    .modal-backdrop {
        display: none;
        position: fixed;
        z-index: 9999;
        inset: 0;
        background: rgba(15, 23, 42, 0.95);
        backdrop-filter: blur(8px);
        justify-content: center;
        align-items: center;
        animation: fadeIn 0.3s ease;
    }

        .modal-backdrop.active {
            display: flex;
        }

    @@keyframes fadeIn {
        from {
            opacity: 0;
        }

        to {
            opacity: 1;
        }
    }

    .modal-content {
        position: relative;
        background: var(--surface);
        border-radius: 24px;
        padding: 0.5rem;
        max-width: 100vw;
        max-height: 100vh;
        width: auto;
        height: auto;
        display: inline-block;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        box-sizing: content-box;
    }

    .modal-main-image {
        width: 600px;
        height: 600px;
        object-fit: contain;
        border-radius: 16px;
        margin-bottom: 1.5rem;
        background: var(--surface-alt);
    }

    .modal-close {
        position: absolute;
        top: 1rem;
        right: 1rem;
        width: 44px;
        height: 44px;
        border-radius: 50%;
        background: rgba(15, 23, 42, 0.8);
        color: white;
        border: none;
        font-size: 1.5rem;
        cursor: pointer;
        transition: var(--transition);
        z-index: 10;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .modal-close:hover {
            background: var(--danger);
            transform: rotate(90deg);
        }

    .no-image {
        position: relative;
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        background: #f3f4f6;
    }

        .no-image .main-image {
            cursor: default !important;
            opacity: 0.5;
            filter: grayscale(0.3);
            pointer-events: none;
        }

    .modal-arrow {
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        width: 50px;
        height: 50px;
        border-radius: 50%;
        background: rgba(15, 23, 42, 0.8);
        color: white;
        border: none;
        font-size: 1.5rem;
        cursor: pointer;
        transition: var(--transition);
        z-index: 10;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .modal-arrow:hover {
            background: var(--primary);
            transform: translateY(-50%) scale(1.1);
        }

        .modal-arrow.left {
            left: 2rem;
        }

        .modal-arrow.right {
            right: 2rem;
        }

    .modal-thumbnails {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        justify-content: center;
        max-width: 80vw;
        overflow-x: auto;
        padding: 10px;
    }

    .modal-thumbnail {
        width: 70px;
        height: 70px;
        object-fit: cover;
        border-radius: 10px;
        border: 3px solid transparent;
        cursor: pointer;
        transition: var(--transition);
        flex-shrink: 0;
    }

        .modal-thumbnail:hover {
            border-color: var(--primary);
            transform: scale(1.1);
        }

        .modal-thumbnail.selected {
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(99, 102,241, 0.3);
        }

    @@media (max-width: 992px) {
        .product-grid {
            grid-template-columns: 1fr;
        }

        .gallery-card {
            position: static;
        }

        .specs-grid {
            grid-template-columns: 1fr;
        }
    }

    @@media (max-width: 768px) {
        .product-page-wrapper {
            padding: 1.5rem 0 3rem 0;
        }

        .product-container {
            padding: 0 1rem;
        }

        .product-meta-bar {
            justify-content: center;
            gap: 1rem;
        }

        .gallery-card,
        .info-card {
            padding: 1.5rem;
        }

        .product-title {
            font-size: 2rem;
            font-weight: 800;
            color: var(--text-primary);
            margin-bottom: 1rem;
            line-height: 1.2;
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            padding-bottom: 0.2em;
            box-decoration-break: clone;
            -webkit-box-decoration-break: clone;
        }

        .product-price {
            font-size: 1.1rem;
        }

        .price-availability {
            flex-direction: column;
            align-items: flex-start;
        }

        .modal-main-image {
            width: 90vw;
            height: 90vw;
            max-width: 500px;
            max-height: 500px;
        }

        .modal-arrow.left {
            left: 0.5rem;
        }

        .modal-arrow.right {
            right: 0.5rem;
        }
    }
    .main-image-wrapper {
        position: relative;
    }
    .main-image-tooltip {
        display: none;
        position: absolute;
        left: 50%;
        bottom: 12px;
        transform: translateX(-50%);
        background: rgba(99,102,241,0.95);
        color: #fff;
        padding: 6px 16px;
        border-radius: 8px;
        font-size: 0.95rem;
        font-weight: 500;
        white-space: nowrap;
        box-shadow: var(--shadow-md);
        pointer-events: none;
        z-index: 10;
        transition: opacity 0.2s;
        opacity: 0;
    }
    .main-image-wrapper:hover .main-image-tooltip,
    .main-image:focus + .main-image-tooltip {
        display: block;
        opacity: 1;
    }

    #similarProductsCarousel .carousel-item {
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 2rem 0;
    }

    similar-products-section {
        margin: 4rem 0 2rem;
        padding: 2.5rem 0;
        background: var(--surface);
        border-radius: 24px;
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--border);
    }

    .similar-products-title {
        font-size: 1.75rem;
        font-weight: 800;
        color: var(--text-primary);
        margin-bottom: 2rem;
        text-align: center;
        background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .similar-products-carousel {
        display: flex;
        flex-direction: row;
        gap: 2rem;
        padding: 1rem 0;
        transition: transform 0.6s ease;
        will-change: transform;
    }

    .similar-products-carousel-outer {
        overflow: hidden;
        padding: 0 1rem;
    }

    .similar-product-card {
        flex: 0 0 260px;
        background: var(--surface-alt);
        border-radius: 16px;
        overflow: hidden;
        box-shadow: var(--shadow-md);
        transition: var(--transition);
        border: 1px solid var(--border);
        text-align: center;
        padding: 1.25rem 1rem;
        animation: floatCard 3s ease-in-out infinite;
    }

        .similar-product-card:hover {
            animation: none;
            transform: translateY(-12px);
            box-shadow: var(--shadow-lg);
            border-color: var(--primary-light);
        }

        .similar-product-card img {
            width: 100%;
            height: 220px;
            object-fit: cover;
            border-radius: 12px;
            margin-bottom: 1rem;
            background: white;
        }

        .similar-product-card h5 {
            font-size: 1.1rem;
            font-weight: 700;
            margin: 0.75rem 0 0.5rem;
            color: var(--text-primary);
            line-height: 1.4;
            min-height: 2.8em;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .similar-product-card p {
            font-size: 1.25rem;
            font-weight: 800;
            margin: 0.5rem 0 1rem;
        }

        .similar-product-card .btn {
            width: 100%;
            padding: 0.75rem;
            font-weight: 600;
            border-radius: 12px;
            transition: var(--transition);
        }

            .similar-product-card .btn:hover {
                transform: scale(1.04);
                box-shadow: 0 8px 24px rgba(99, 102, 241, 0.25);
            }

    @@keyframes floatCard {
        0% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
        100% { transform: translateY(0); }
    }

    .similar-products-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 2rem;
        padding: 1rem 0;
    }

    .price-missing {
        font-size: 0.95rem;
        font-weight: 600;
        color: #b42318;
        background: #fef3f2;
        border: 1px dashed #f5c2c7;
        border-radius: 12px;
        padding: 0.75rem 1.25rem;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        letter-spacing: -0.01em;
        white-space: nowrap;
        margin-bottom: 1rem;
    }

        .price-missing i {
            font-size: 1.1rem;
            opacity: 0.85;
        }

    .specs-section-upgraded {
        margin-top: 2.5rem;
        padding: 2.5rem 2rem;
        border-radius: 32px;
        background: rgba(40, 48, 72, 0.55);
        box-shadow: 0 12px 40px 0 rgba(99,102,241,0.18), 0 2px 12px 0 rgba(236,72,153,0.10);
        backdrop-filter: blur(18px) saturate(160%);
        border: 2px solid rgba(99,102,241,0.18);
        position: relative;
        overflow: hidden;
        transition: box-shadow 0.3s, background 0.3s;
    }

        .specs-section-upgraded::before {
            content: "";
            position: absolute;
            inset: 0;
            background: linear-gradient(120deg, #a5b4fc 0%, #fbcfe8 100%);
            opacity: 0.08;
            z-index: 0;
            pointer-events: none;
        }

    .specs-header {
        display: flex;
        align-items: center;
        margin-bottom: 2.2rem;
        gap: 1.2rem;
        position: relative;
        z-index: 1;
    }

    .specs-accent {
        width: 8px;
        height: 44px;
        border-radius: 8px;
        background: linear-gradient(180deg, #6366f1 0%, #ec4899 100%);
        box-shadow: 0 4px 16px #6366f144;
        animation: accentPulse 2.5s infinite alternate;
    }
    @@keyframes accentPulse {
        0%

    {
        box-shadow: 0 4px 16px #6366f144;
    }

    100% {
        box-shadow: 0 8px 32px #ec489944;
    }

    }

    .specs-title {
        font-size: 1.7rem;
        font-weight: 900;
        letter-spacing: -0.5px;
        display: flex;
        align-items: center;
        gap: 0.8rem;
        background: linear-gradient(90deg, #6366f1 0%, #ec4899 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        filter: drop-shadow(0 2px 8px #6366f122);
    }

    .specs-grid-upgraded {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 2rem;
        position: relative;
        z-index: 1;
    }

    .spec-card {
        display: flex;
        align-items: flex-start;
        gap: 1.3rem;
        background: rgba(255,255,255,0.18);
        border-radius: 20px;
        padding: 1.5rem 1.2rem;
        box-shadow: 0 4px 18px 0 rgba(99,102,241,0.10);
        border: 2px solid rgba(99,102,241,0.10);
        transition: box-shadow 0.25s, transform 0.25s, border 0.25s, background 0.25s;
        min-height: 90px;
        position: relative;
        overflow: hidden;
        cursor: pointer;
    }

        .spec-card:hover {
            box-shadow: 0 12px 32px 0 rgba(99,102,241,0.18);
            border: 2px solid #6366f1;
            transform: translateY(-4px) scale(1.04);
            background: rgba(255,255,255,0.35);
        }

    .spec-icon {
        font-size: 2rem;
        color: #fff;
        background: linear-gradient(135deg, #6366f1 0%, #ec4899 100%);
        border-radius: 12px;
        padding: 0.7rem 1rem;
        display: flex;
        align-items: center;
        justify-content: center;
        min-width: 44px;
        min-height: 44px;
        box-shadow: 0 2px 12px #6366f122;
        transition: background 0.25s, color 0.25s;
    }

    .spec-card:hover .spec-icon {
        background: linear-gradient(135deg, #ec4899 0%, #6366f1 100%);
        color: #fff;
    }

    .spec-label-upgraded {
        font-size: 0.9rem;
        color: #a5b4fc;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-bottom: 0.3rem;
        text-shadow: 0 1px 2px #6366f122;
    }

    .spec-value-upgraded {
        font-size: 1.18rem;
        color: #fff;
        font-weight: 800;
        letter-spacing: -0.01em;
        word-break: break-word;
        text-shadow: 0 2px 8px #6366f122;
    }

    .actions-section {
        padding-top: 2rem;
        border-top: 2px solid var(--border);
    }

    .btn-back {
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        padding: 1rem 2rem;
        border-radius: 12px;
        font-weight: 600;
        font-size: 1rem;
        border: 2px solid var(--primary);
        background: var(--surface);
        color: var(--primary);
        text-decoration: none;
        transition: var(--transition);
        box-shadow: var(--shadow-sm);
    }

        .btn-back:hover {
            background: var(--primary);
            color: white;
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }

    .modal-backdrop {
        display: none;
        position: fixed;
        z-index: 9999;
        inset: 0;
        background: rgba(15, 23, 42, 0.95);
        backdrop-filter: blur(8px);
        justify-content: center;
        align-items: center;
        animation: fadeIn 0.3s ease;
    }

        .modal-backdrop.active {
            display: flex;
        }

    @@keyframes fadeIn {
        from {
            opacity: 0;
        }

        to {
            opacity: 1;
        }
    }

    .modal-content {
        position: relative;
        background: var(--surface);
        border-radius: 24px;
        padding: 0.5rem;
        max-width: 100vw;
        max-height: 100vh;
        width: auto;
        height: auto;
        display: inline-block;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        box-sizing: content-box;
    }

    .modal-main-image {
        width: 600px;
        height: 600px;
        object-fit: contain;
        border-radius: 16px;
        margin-bottom: 1.5rem;
        background: var(--surface-alt);
    }

    .modal-close {
        position: absolute;
        top: 1rem;
        right: 1rem;
        width: 44px;
        height: 44px;
        border-radius: 50%;
        background: rgba(15, 23, 42, 0.8);
        color: white;
        border: none;
        font-size: 1.5rem;
        cursor: pointer;
        transition: var(--transition);
        z-index: 10;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .modal-close:hover {
            background: var(--danger);
            transform: rotate(90deg);
        }

    .modal-arrow {
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        width: 50px;
        height: 50px;
        border-radius: 50%;
        background: rgba(15, 23, 42, 0.8);
        color: white;
        border: none;
        font-size: 1.5rem;
        cursor: pointer;
        transition: var(--transition);
        z-index: 10;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .modal-arrow:hover {
            background: var(--primary);
            transform: translateY(-50%) scale(1.1);
        }

        .modal-arrow.left {
            left: 2rem;
        }

        .modal-arrow.right {
            right: 2rem;
        }

    .modal-thumbnails {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        justify-content: center;
        max-width: 80vw;
        overflow-x: auto;
        padding: 10px;
    }

    .modal-thumbnail {
        width: 70px;
        height: 70px;
        object-fit: cover;
        border-radius: 10px;
        border: 3px solid transparent;
        cursor: pointer;
        transition: var(--transition);
        flex-shrink: 0;
    }

        .modal-thumbnail:hover {
            border-color: var(--primary);
            transform: scale(1.1);
        }

        .modal-thumbnail.selected {
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(99, 102,241, 0.3);
        }

    @@media (max-width: 992px) {
        .product-grid {
            grid-template-columns: 1fr;
        }

        .gallery-card {
            position: static;
        }

        .specs-grid {
            grid-template-columns: 1fr;
        }
    }

    @@media (max-width: 768px) {
        .product-page-wrapper {
            padding: 1.5rem 0 3rem 0;
        }

        .product-container {
            padding: 0 1rem;
        }

        .product-meta-bar {
            justify-content: center;
            gap: 1rem;
        }

        .gallery-card,
        .info-card {
            padding: 1.5rem;
        }

        .product-title {
            font-size: 2rem;
            font-weight: 800;
            color: var(--text-primary);
            margin-bottom: 1rem;
            line-height: 1.2;
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            padding-bottom: 0.2em;
            box-decoration-break: clone;
            -webkit-box-decoration-break: clone;
        }

        .product-price {
            font-size: 1.1rem;
        }

        .price-availability {
            flex-direction: column;
            align-items: flex-start;
        }

        .modal-main-image {
            width: 90vw;
            height: 90vw;
            max-width: 500px;
            max-height: 500px;
        }

        .modal-arrow.left {
            left: 0.5rem;
        }

        .modal-arrow.right {
            right: 0.5rem;
        }
    }
    .main-image-wrapper {
        position: relative;
    }
    .main-image-tooltip {
        display: none;
        position: absolute;
        left: 50%;
        bottom: 12px;
        transform: translateX(-50%);
        background: rgba(99,102,241,0.95);
        color: #fff;
        padding: 6px 16px;
        border-radius: 8px;
        font-size: 0.95rem;
        font-weight: 500;
        white-space: nowrap;
        box-shadow: var(--shadow-md);
        pointer-events: none;
        z-index: 10;
        transition: opacity 0.2s;
        opacity: 0;
    }
    .main-image-wrapper:hover .main-image-tooltip,
    .main-image:focus + .main-image-tooltip {
        display: block;
        opacity: 1;
    }

    #similarProductsCarousel .carousel-item {
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 2rem 0;
    }

    similar-products-section {
        margin: 4rem 0 2rem;
        padding: 2.5rem 0;
        background: var(--surface);
        border-radius: 24px;
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--border);
    }

    .similar-products-title {
        font-size: 1.75rem;
        font-weight: 800;
        color: var(--text-primary);
        margin-bottom: 2rem;
        text-align: center;
        background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .similar-products-carousel {
        display: flex;
        flex-direction: row;
        gap: 2rem;
        padding: 1rem 0;
        transition: transform 0.6s ease;
        will-change: transform;
    }

    .similar-products-carousel-outer {
        overflow: hidden;
        padding: 0 1rem;
    }

    .similar-product-card {
        flex: 0 0 260px;
        background: var(--surface-alt);
        border-radius: 16px;
        overflow: hidden;
        box-shadow: var(--shadow-md);
        transition: var(--transition);
        border: 1px solid var(--border);
        text-align: center;
        padding: 1.25rem 1rem;
        animation: floatCard 3s ease-in-out infinite;
    }

        .similar-product-card:hover {
            animation: none;
            transform: translateY(-12px);
            box-shadow: var(--shadow-lg);
            border-color: var(--primary-light);
        }

        .similar-product-card img {
            width: 100%;
            height: 220px;
            object-fit: cover;
            border-radius: 12px;
            margin-bottom: 1rem;
            background: white;
        }

        .similar-product-card h5 {
            font-size: 1.1rem;
            font-weight: 700;
            margin: 0.75rem 0 0.5rem;
            color: var(--text-primary);
            line-height: 1.4;
            min-height: 2.8em;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .similar-product-card p {
            font-size: 1.25rem;
            font-weight: 800;
            margin: 0.5rem 0 1rem;
        }

        .similar-product-card .btn {
            width: 100%;
            padding: 0.75rem;
            font-weight: 600;
            border-radius: 12px;
            transition: var(--transition);
        }

            .similar-product-card .btn:hover {
                transform: scale(1.04);
                box-shadow: 0 8px 24px rgba(99, 102, 241, 0.25);
            }

    @@keyframes floatCard {
        0% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
        100% { transform: translateY(0); }
    }

    .similar-products-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 2rem;
        padding: 1rem 0;
    }

    .price-missing {
        font-size: 0.95rem;
        font-weight: 600;
        color: #b42318;
        background: #fef3f2;
        border: 1px dashed #f5c2c7;
        border-radius: 12px;
        padding: 0.75rem 1.25rem;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        letter-spacing: -0.01em;
        white-space: nowrap;
    }

        .price-missing i {
            font-size: 1.1rem;
            opacity: 0.85;
        }

    .specs-section-upgraded {
        margin-top: 2.5rem;
        padding: 2.5rem 2rem;
        border-radius: 28px;
        background: rgba(255,255,255,0.7);
        box-shadow: 0 8px 32px 0 rgba(99,102,241,0.10), 0 1.5px 8px 0 rgba(236,72,153,0.08);
        backdrop-filter: blur(12px);
        border: 1.5px solid #e0e7ff;
        position: relative;
    }

    .specs-header {
        display: flex;
        align-items: center;
        margin-bottom: 2rem;
        gap: 1rem;
    }

    .specs-accent {
        width: 6px;
        height: 38px;
        border-radius: 6px;
        background: linear-gradient(180deg, #6366f1 0%, #ec4899 100%);
        box-shadow: 0 2px 8px #6366f133;
    }

    .specs-title {
        font-size: 1.45rem;
        font-weight: 900;
        color: #6366f1;
        letter-spacing: -0.5px;
        display: flex;
        align-items: center;
        gap: 0.7rem;
        background: linear-gradient(90deg, #6366f1 0%, #ec4899 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .specs-grid-upgraded {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 1.5rem;
    }

    .spec-card {
        display: flex;
        align-items: flex-start;
        gap: 1.1rem;
        background: rgba(243,244,246,0.85);
        border-radius: 18px;
        padding: 1.25rem 1.1rem;
        box-shadow: 0 2px 12px 0 rgba(99,102,241,0.07);
        border: 1.5px solid #e0e7ff;
        transition: box-shadow 0.2s, transform 0.2s, border 0.2s;
        min-height: 80px;
        position: relative;
    }

        .spec-card:hover {
            box-shadow: 0 6px 24px 0 rgba(99,102,241,0.13);
            border: 1.5px solid #6366f1;
            transform: translateY(-2px) scale(1.03);
            background: rgba(255,255,255,0.95);
        }

    .spec-icon {
        font-size: 1.6rem;
        color: #6366f1;
        background: linear-gradient(135deg, #ede9fe 0%, #fce7f3 100%);
        border-radius: 10px;
        padding: 0.5rem 0.7rem;
        display: flex;
        align-items: center;
        justify-content: center;
        min-width: 38px;
        min-height: 38px;
        box-shadow: 0 1px 6px #6366f122;
    }

    .spec-label-upgraded {
        font-size: 0.82rem;
        color: #64748b;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-bottom: 0.2rem;
    }

    .spec-value-upgraded {
        font-size: 1.13rem;
        color: #0f172a;
        font-weight: 700;
        letter-spacing: -0.01em;
        word-break: break-word;
    }

    .actions-section {
        padding-top: 2rem;
        border-top: 2px solid var(--border);
    }

    .btn-back {
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        padding: 1rem 2rem;
        border-radius: 12px;
        font-weight: 600;
        font-size: 1rem;
        border: 2px solid var(--primary);
        background: var(--surface);
        color: var(--primary);
        text-decoration: none;
        transition: var(--transition);
        box-shadow: var(--shadow-sm);
    }

        .btn-back:hover {
            background: var(--primary);
            color: white;
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }

    .modal-backdrop {
        display: none;
        position: fixed;
        z-index: 9999;
        inset: 0;
        background: rgba(15, 23, 42, 0.95);
        backdrop-filter: blur(8px);
        justify-content: center;
        align-items: center;
        animation: fadeIn 0.3s ease;
    }

        .modal-backdrop.active {
            display: flex;
        }

    @@keyframes fadeIn {
        from {
            opacity: 0;
        }

        to {
            opacity: 1;
        }
    }

    .modal-content {
        position: relative;
        background: var(--surface);
        border-radius: 24px;
        padding: 0.5rem;
        max-width: 100vw;
        max-height: 100vh;
        width: auto;
        height: auto;
        display: inline-block;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        box-sizing: content-box;
    }

    .modal-main-image {
        width: 600px;
        height: 600px;
        object-fit: contain;
        border-radius: 16px;
        margin-bottom: 1.5rem;
        background: var(--surface-alt);
    }

    .modal-close {
        position: absolute;
        top: 1rem;
        right: 1rem;
        width: 44px;
        height: 44px;
        border-radius: 50%;
        background: rgba(15, 23, 42, 0.8);
        color: white;
        border: none;
        font-size: 1.5rem;
        cursor: pointer;
        transition: var(--transition);
        z-index: 10;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .modal-close:hover {
            background: var(--danger);
            transform: rotate(90deg);
        }

    .modal-arrow {
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        width: 50px;
        height: 50px;
        border-radius: 50%;
        background: rgba(15, 23, 42, 0.8);
        color: white;
        border: none;
        font-size: 1.5rem;
        cursor: pointer;
        transition: var(--transition);
        z-index: 10;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .modal-arrow:hover {
            background: var(--primary);
            transform: translateY(-50%) scale(1.1);
        }

        .modal-arrow.left {
            left: 2rem;
        }

        .modal-arrow.right {
            right: 2rem;
        }

    .modal-thumbnails {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        justify-content: center;
        max-width: 80vw;
        overflow-x: auto;
        padding: 10px;
    }

    .modal-thumbnail {
        width: 70px;
        height: 70px;
        object-fit: cover;
        border-radius: 10px;
        border: 3px solid transparent;
        cursor: pointer;
        transition: var(--transition);
        flex-shrink: 0;
    }

        .modal-thumbnail:hover {
            border-color: var(--primary);
            transform: scale(1.1);
        }

        .modal-thumbnail.selected {
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(99, 102,241, 0.3);
        }

    @@media (max-width: 992px) {
        .product-grid {
            grid-template-columns: 1fr;
        }

        .gallery-card {
            position: static;
        }

        .specs-grid {
            grid-template-columns: 1fr;
        }
    }

    @@media (max-width: 768px) {
        .product-page-wrapper {
            padding: 1.5rem 0 3rem 0;
        }

        .product-container {
            padding: 0 1rem;
        }

        .product-meta-bar {
            justify-content: center;
            gap: 1rem;
        }

        .gallery-card,
        .info-card {
            padding: 1.5rem;
        }

        .product-title {
            font-size: 2rem;
            font-weight: 800;
            color: var(--text-primary);
            margin-bottom: 1rem;
            line-height: 1.2;
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            padding-bottom: 0.2em;
            box-decoration-break: clone;
            -webkit-box-decoration-break: clone;
        }

        .product-price {
            font-size: 1.1rem;
        }

        .price-availability {
            flex-direction: column;
            align-items: flex-start;
        }

        .modal-main-image {
            width: 90vw;
            height: 90vw;
            max-width: 500px;
            max-height: 500px;
        }

        .modal-arrow.left {
            left: 0.5rem;
        }

        .modal-arrow.right {
            right: 0.5rem;
        }
    }
    .main-image-wrapper {
        position: relative;
    }
    .main-image-tooltip {
        display: none;
        position: absolute;
        left: 50%;
        bottom: 12px;
        transform: translateX(-50%);
        background: rgba(99,102,241,0.95);
        color: #fff;
        padding: 6px 16px;
        border-radius: 8px;
        font-size: 0.95rem;
        font-weight: 500;
        white-space: nowrap;
        box-shadow: var(--shadow-md);
        pointer-events: none;
        z-index: 10;
        transition: opacity 0.2s;
        opacity: 0;
    }
    .main-image-wrapper:hover .main-image-tooltip,
    .main-image:focus + .main-image-tooltip {
        display: block;
        opacity: 1;
    }

    #similarProductsCarousel .carousel-item {
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 2rem 0;
    }

    similar-products-section {
        margin: 4rem 0 2rem;
        padding: 2.5rem 0;
        background: var(--surface);
        border-radius: 24px;
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--border);
    }

    .similar-products-title {
        font-size: 1.75rem;
        font-weight: 800;
        color: var(--text-primary);
        margin-bottom: 2rem;
        text-align: center;
        background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .similar-products-carousel {
        display: flex;
        flex-direction: row;
        gap: 2rem;
        padding: 1rem 0;
        transition: transform 0.6s ease;
        will-change: transform;
    }

    .similar-products-carousel-outer {
        overflow: hidden;
        padding: 0 1rem;
    }

    .similar-product-card {
        flex: 0 0 260px;
        background: var(--surface-alt);
        border-radius: 16px;
        overflow: hidden;
        box-shadow: var(--shadow-md);
        transition: var(--transition);
        border: 1px solid var(--border);
        text-align: center;
        padding: 1.25rem 1rem;
        animation: floatCard 3s ease-in-out infinite;
    }

        .similar-product-card:hover {
            animation: none;
            transform: translateY(-12px);
            box-shadow: var(--shadow-lg);
            border-color: var(--primary-light);
        }

        .similar-product-card img {
            width: 100%;
            height: 220px;
            object-fit: cover;
            border-radius: 12px;
            margin-bottom: 1rem;
            background: white;
        }

        .similar-product-card h5 {
            font-size: 1.1rem;
            font-weight: 700;
            margin: 0.75rem 0 0.5rem;
            color: var(--text-primary);
            line-height: 1.4;
            min-height: 2.8em;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .similar-product-card p {
            font-size: 1.25rem;
            font-weight: 800;
            margin: 0.5rem 0 1rem;
        }

        .similar-product-card .btn {
            width: 100%;
            padding: 0.75rem;
            font-weight: 600;
            border-radius: 12px;
            transition: var(--transition);
        }

            .similar-product-card .btn:hover {
                transform: scale(1.04);
                box-shadow: 0 8px 24px rgba(99, 102, 241, 0.25);
            }

    @@keyframes floatCard {
        0% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
        100% { transform: translateY(0); }
    }

    .similar-products-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 2rem;
        padding: 1rem 0;
    }

    .price-missing {
        font-size: 0.95rem;
        font-weight: 600;
        color: #b42318;
        background: #fef3f2;
        border: 1px dashed #f5c2c7;
        border-radius: 12px;
        padding: 0.75rem 1.25rem;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        letter-spacing: -0.01em;
        white-space: nowrap;
    }

        .price-missing i {
            font-size: 1.1rem;
            opacity: 0.85;
        }

    .specs-section-upgraded {
        margin-top: 2.5rem;
        padding: 2.5rem 2rem;
        border-radius: 28px;
        background: rgba(255,255,255,0.7);
        box-shadow: 0 8px 32px 0 rgba(99,102,241,0.10), 0 1.5px 8px 0 rgba(236,72,153,0.08);
        backdrop-filter: blur(12px);
        border: 1.5px solid #e0e7ff;
        position: relative;
    }

    .specs-header {
        display: flex;
        align-items: center;
        margin-bottom: 2rem;
        gap: 1rem;
    }

    .specs-accent {
        width: 6px;
        height: 38px;
        border-radius: 6px;
        background: linear-gradient(180deg, #6366f1 0%, #ec4899 100%);
        box-shadow: 0 2px 8px #6366f133;
    }

    .specs-title {
        font-size: 1.45rem;
        font-weight: 900;
        color: #6366f1;
        letter-spacing: -0.5px;
        display: flex;
        align-items: center;
        gap: 0.7rem;
        background: linear-gradient(90deg, #6366f1 0%, #ec4899 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .specs-grid-upgraded {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 1.5rem;
    }

    .spec-card {
        display: flex;
        align-items: flex-start;
        gap: 1.1rem;
        background: rgba(243,244,246,0.85);
        border-radius: 18px;
        padding: 1.25rem 1.1rem;
        box-shadow: 0 2px 12px 0 rgba(99,102,241,0.07);
        border: 1.5px solid #e0e7ff;
        transition: box-shadow 0.2s, transform 0.2s, border 0.2s;
        min-height: 80px;
        position: relative;
    }

        .spec-card:hover {
            box-shadow: 0 6px 24px 0 rgba(99,102,241,0.13);
            border: 1.5px solid #6366f1;
            transform: translateY(-2px) scale(1.03);
            background: rgba(255,255,255,0.95);
        }

    .similar-product-card .no-image {
        width: 100%;
        aspect-ratio: 1;
        border-radius: 12px;
        overflow: hidden;
        background: var(--surface-alt);
        position: relative;
        margin-bottom: 1rem;
        min-height: 180px;
        max-height: 220px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .similar-product-card .no-image .main-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            cursor: default !important;
            opacity: 0.5;
            filter: grayscale(0.3);
            pointer-events: none;
            border-radius: 12px;
            background: #f3f4f6;
        }
    .spec-icon {
        font-size: 1.6rem;
        color: #6366f1;
        background: linear-gradient(135deg, #ede9fe 0%, #fce7f3 100%);
        border-radius: 10px;
        padding: 0.5rem 0.7rem;
        display: flex;
        align-items: center;
        justify-content: center;
        min-width: 38px;
        min-height: 38px;
        box-shadow: 0 1px 6px #6366f122;
    }

    .spec-label-upgraded {
        font-size: 0.82rem;
        color: #64748b;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-bottom: 0.2rem;
    }

    .spec-value-upgraded {
        font-size: 1.13rem;
        color: #0f172a;
        font-weight: 700;
        letter-spacing: -0.01em;
        word-break: break-word;
    }

    .actions-section {
        padding-top: 2rem;
        border-top: 2px solid var(--border);
    }

    .btn-back {
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        padding: 1rem 2rem;
        border-radius: 12px;
        font-weight: 600;
        font-size: 1rem;
        border: 2px solid var(--primary);
        background: var(--surface);
        color: var(--primary);
        text-decoration: none;
        transition: var(--transition);
        box-shadow: var(--shadow-sm);
    }

        .btn-back:hover {
            background: var(--primary);
            color: white;
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }

    .modal-backdrop {
        display: none;
        position: fixed;
        z-index: 9999;
        inset: 0;
        background: rgba(15, 23, 42, 0.95);
        backdrop-filter: blur(8px);
        justify-content: center;
        align-items: center;
        animation: fadeIn 0.3s ease;
    }

        .modal-backdrop.active {
            display: flex;
        }

    @@keyframes fadeIn {
        from {
            opacity: 0;
        }

        to {
            opacity: 1;
        }
    }

    .modal-content {
        position: relative;
        background: var(--surface);
        border-radius: 24px;
        padding: 0.5rem;
        max-width: 100vw;
        max-height: 100vh;
        width: auto;
        height: auto;
        display: inline-block;
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        box-sizing: content-box;
    }

    .modal-main-image {
        width: 600px;
        height: 600px;
        object-fit: contain;
        border-radius: 16px;
        margin-bottom: 1.5rem;
        background: var(--surface-alt);
    }

    .modal-close {
        position: absolute;
        top: 1rem;
        right: 1rem;
        width: 44px;
        height: 44px;
        border-radius: 50%;
        background: rgba(15, 23, 42, 0.8);
        color: white;
        border: none;
        font-size: 1.5rem;
        cursor: pointer;
        transition: var(--transition);
        z-index: 10;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .modal-close:hover {
            background: var(--danger);
            transform: rotate(90deg);
        }

    .modal-arrow {
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        width: 50px;
        height: 50px;
        border-radius: 50%;
        background: rgba(15, 23, 42, 0.8);
        color: white;
        border: none;
        font-size: 1.5rem;
        cursor: pointer;
        transition: var(--transition);
        z-index: 10;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .modal-arrow:hover {
            background: var(--primary);
            transform: translateY(-50%) scale(1.1);
        }

        .modal-arrow.left {
            left: 2rem;
        }

        .modal-arrow.right {
            right: 2rem;
        }

    .modal-thumbnails {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        justify-content: center;
        max-width: 80vw;
        overflow-x: auto;
        padding: 10px;
    }

    .modal-thumbnail {
        width: 70px;
        height: 70px;
        object-fit: cover;
        border-radius: 10px;
        border: 3px solid transparent;
        cursor: pointer;
        transition: var(--transition);
        flex-shrink: 0;
    }

        .modal-thumbnail:hover {
            border-color: var(--primary);
            transform: scale(1.1);
        }

        .modal-thumbnail.selected {
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(99, 102,241, 0.3);
        }

    @@media (max-width: 992px) {
        .product-grid {
            grid-template-columns: 1fr;
        }

        .gallery-card {
            position: static;
        }

        .specs-grid {
            grid-template-columns: 1fr;
        }
    }

    @@media (max-width: 768px) {
        .product-page-wrapper {
            padding: 1.5rem 0 3rem 0;
        }

        .product-container {
            padding: 0 1rem;
        }

        .product-meta-bar {
            justify-content: center;
            gap: 1rem;
        }

        .gallery-card,
        .info-card {
            padding: 1.5rem;
        }

        .product-title {
            font-size: 2rem;
            font-weight: 800;
            color: var(--text-primary);
            margin-bottom: 1rem;
            line-height: 1.2;
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            padding-bottom: 0.2em;
            box-decoration-break: clone;
            -webkit-box-decoration-break: clone;
        }

        .product-price {
            font-size: 1.1rem;
        }

        .price-availability {
            flex-direction: column;
            align-items: flex-start;
        }

        .modal-main-image {
            width: 90vw;
            height: 90vw;
            max-width: 500px;
            max-height: 500px;
        }

        .modal-arrow.left {
            left: 0.5rem;
        }

        .modal-arrow.right {
            right: 0.5rem;
        }
    }
    .main-image-wrapper {
        position: relative;
    }
    .main-image-tooltip {
        display: none;
        position: absolute;
        left: 50%;
        bottom: 12px;
        transform: translateX(-50%);
        background: rgba(99,102,241,0.95);
        color: #fff;
        padding: 6px 16px;
        border-radius: 8px;
        font-size: 0.95rem;
        font-weight: 500;
        white-space: nowrap;
        box-shadow: var(--shadow-md);
        pointer-events: none;
        z-index: 10;
        transition: opacity 0.2s;
        opacity: 0;
    }
    .main-image-wrapper:hover .main-image-tooltip,
    .main-image:focus + .main-image-tooltip {
        display: block;
        opacity: 1;
    }

    #similarProductsCarousel .carousel-item {
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 2rem 0;
    }

    similar-products-section {
        margin: 4rem 0 2rem;
        padding: 2.5rem 0;
        background: var(--surface);
        border-radius: 24px;
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--border);
    }

    .similar-products-title {
        font-size: 1.75rem;
        font-weight: 800;
        color: var(--text-primary);
        margin-bottom: 2rem;
        text-align: center;
        background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .similar-products-carousel {
        display: flex;
        flex-direction: row;
        gap: 2rem;
        padding: 1rem 0;
        transition: transform 0.6s ease;
        will-change: transform;
    }

    .similar-products-carousel-outer {
        overflow: hidden;
        padding: 0 1rem;
    }

    .similar-product-card {
        flex: 0 0 260px;
        background: var(--surface-alt);
        border-radius: 16px;
        overflow: hidden;
        box-shadow: var(--shadow-md);
        transition: var(--transition);
        border: 1px solid var(--border);
        text-align: center;
        padding: 1.25rem 1rem;
        animation: floatCard 3s ease-in-out infinite;
    }

        .similar-product-card:hover {
            animation: none;
            transform: translateY(-12px);
            box-shadow: var(--shadow-lg);
            border-color: var(--primary-light);
        }

        .similar-product-card img {
            width: 100%;
            height: 220px;
            object-fit: cover;
            border-radius: 12px;
            margin-bottom: 1rem;
            background: white;
        }

        .similar-product-card h5 {
            font-size: 1.1rem;
            font-weight: 700;
            margin: 0.75rem 0 0.5rem;
            color: var(--text-primary);
            line-height: 1.4;
            min-height: 2.8em;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .similar-product-card p {
            font-size: 1.25rem;
            font-weight: 800;
            margin: 0.5rem 0 1rem;
        }

        .similar-product-card .btn {
            width: 100%;
            padding: 0.75rem;
            font-weight: 600;
            border-radius: 12px;
            transition: var(--transition);
        }

            .similar-product-card .btn:hover {
                transform: scale(1.04);
                box-shadow: 0 8px 24px rgba(99, 102, 241, 0.25);
            }

    @@keyframes floatCard {
        0% { transform: translateY(0); }
        50% { transform: translateY(-10px); }
        100% { transform: translateY(0); }
    }

    .similar-products-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 2rem;
        padding: 1rem 0;
    }

    .price-missing {
        font-size: 0.95rem;
        font-weight: 600;
        color: #b42318;
        background: #fef3f2;
        border: 1px dashed #f5c2c7;
        border-radius: 12px;
        padding: 0.75rem 1.25rem;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        letter-spacing: -0.01em;
        white-space: nowrap;
    }

        .price-missing i {
            font-size: 1.1rem;
            opacity: 0.85;
        }

    .specs-section-upgraded {
        margin-top: 2.5rem;
        padding: 2.5rem 2rem;
        border-radius: 28px;
        background: rgba(255,255,255,0.7);
        box-shadow: 0 8px 32px 0 rgba(99,102,241,0.10), 0 1.5px 8px 0 rgba(236,72,153,0.08);
        backdrop-filter: blur(12px);
        border: 1.5px solid #e0e7ff;
        position: relative;
    }

    .specs-header {
        display: flex;
        align-items: center;
        margin-bottom: 2rem;
        gap: 1rem;
    }

    .specs-accent {
        width: 6px;
        height: 38px;
        border-radius: 6px;
        background: linear-gradient(180deg, #6366f1 0%, #ec4899 100%);
        box-shadow: 0 2px 8px #6366f133;
    }

    .specs-title {
        font-size: 1.45rem;
        font-weight: 900;
        color: #6366f1;
        letter-spacing: -0.5px;
        display: flex;
        align-items: center;
        gap: 0.7rem;
        background: linear-gradient(90deg, #6366f1 0%, #ec4899 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .specs-grid-upgraded {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 1.5rem;
    }

    .spec-card {
        display: flex;
        align-items: flex-start;
        gap: 1.1rem;
        background: rgba(243,244,246,0.85);
        border-radius: 18px;
        padding: 1.25rem 1.1rem;
        box-shadow: 0 2px 12px 0 rgba(99,102,241,0.07);
        border: 1.5px solid #e0e7ff;
        transition: box-shadow 0.2s, transform 0.2s, border 0.2s;
        min-height: 80px;
        position: relative;
    }

        .spec-card:hover {
            box-shadow: 0 6px 24px 0 rgba(99,102,241,0.13);
            border: 1.5px solid #6366f1;
            transform: translateY(-2px) scale(1.03);
            background: rgba(255,255,255,0.95);
        }

    .spec-icon {
        font-size: 1.6rem;
        color: #6366f1;
        background: linear-gradient(135deg, #ede9fe 0%, #fce7f3 100%);
        border-radius: 10px;
        padding: 0.5rem 0.7rem;
        display: flex;
        align-items: center;
        justify-content: center;
        min-width: 38px;
        min-height: 38px;
        box-shadow: 0 1px 6px #6366f122;
    }

    .spec-label-upgraded {
        font-size: 0.82rem;
        color: #64748b;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-bottom: 0.2rem;
    }

    .spec-value-upgraded {
        font-size: 1.13rem;
        color: #0f172a;
        font-weight: 700;
        letter-spacing: -0.01em;
        word-break: break-word;
    }

    .actions-section {
        padding-top: 2rem;
        border-top: 2px solid var(--border);
    }

    .btn-back {
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        padding: 1rem 2rem;
        border-radius: 12px;
        font-weight: 600;
        font-size: 1rem;
        border: 2px solid var(--primary);
        background: var(--surface);
        color: var(--primary);
        text-decoration: none;
        transition: var(--transition);
        box-shadow: var(--shadow-sm);
    }

        .btn-back:hover {
            background: var(--primary);
            color: white;
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }

    
</style>

<div class="product-page-wrapper">
    <div class="product-container">

        <div class="product-meta-bar">
            @if (Model.Id.HasValue)
            {
                <div class="meta-item">
                    <svg viewBox="0 0 20 20"><path d="M2 10a2 2 0 012-2h2V6a2 2 0 012-2h4a2 2 0 012 2v2h2a2 2 0 012 2v4a2 2 0 01-2 2h-2v2a2 2 0 01-2 2h-4a2 2 0 01-2-2v-2H4a2 2 0 01-2-2v-4zm4-4v2h8V6a1 1 0 00-1-1h-6a1 1 0 00-1 1zm10 4h-2v4h2v-4zm-4 6v-2H8v2h4zm-6-6H4v4h2v-4z" /></svg>
                    ID - @Model.Id.Value.ToString("N0")
                </div>
                <span class="meta-sep">|</span>
            }
            @if (Model.ViewCount.HasValue)
            {
                <div class="meta-item">
                    <svg viewBox="0 0 20 20"><path d="M10 3c-5 0-9 5.25-9 7s4 7 9 7 9-5.25 9-7-4-7-9-7zm0 12c-3.87 0-7-3.13-7-5s3.13-5 7-5 7 3.13 7 5-3.13 5-7 5zm0-8a3 3 0 100 6 3 3 0 000-6zm0 4.5A1.5 1.5 0 1110 7a1.5 1.5 0 010 3z" /></svg>
                    @Model.ViewCount.Value.ToString("N0")
                </div>
                <span class="meta-sep">|</span>
            }
            @if (Model.CreateDate.HasValue)
            {
                <div class="meta-item">
                    <svg viewBox="0 0 20 20"><path d="M6 2a1 1 0 012 0v1h4V2a1 1 0 112 0v1a3 3 0 013 3v10a3 3 0 01-3 3H6a3 3 0 01-3-3V6a3 3 0 013-3V2zm8 4H6a1 1 0 00-1 1v10a1 1 0 001 1h8a1 1 0 001-1V7a1 1 0 00-1-1zm-4 2a1 1 0 110 2 1 1 0 010-2z" /></svg>
                    @Model.CreateDate.Value.ToString("dd MMM yy, HH:mm", new System.Globalization.CultureInfo("ka-GE"))
                </div>
            }
        </div>

        <div class="product-grid">

            <div class="gallery-card">
                <h1 class="product-title">@Model.Title</h1>
                <span class="product-code">📦 კოდი: @Model.Code</span>

                @if (Model.Images != null && Model.Images.Any())
                {
                    <div class="main-image-wrapper">
                        <img id="mainImage"
                             src="@Model.Images.First().FileUrl"
                             class="main-image"
                             alt="@Model.Title"
                             tabindex="0"
                             onclick="openModal(selectedIndex)"
                             onmouseover="showTooltip()"
                             onmouseout="hideTooltip()" />
                        <div class="main-image-tooltip" id="mainImageTooltip">დააკლიკეთ გასადიდებლად 🔍</div>
                    </div>

                    @if (Model.Images.Count > 1)
                    {
                        <div class="thumbnail-grid">
                            @for (int i = 0; i < Model.Images.Count; i++)
                            {
                                var img = Model.Images[i];
                                <div class="thumbnail @(i == 0 ? "active" : "")"
                                     data-index="@i"
                                     onclick="changeImage(@i)">
                                    <img src="@img.FileUrl" alt="@img.FileName" />
                                </div>
                            }
                        </div>
                    }
                }
                else if (!string.IsNullOrEmpty(Model.ProductImageUrl))
                {
                    <div class="main-image-wrapper">
                        <img src="@Model.ProductImageUrl" class="main-image" alt="@Model.Title" />
                    </div>
                }
                else
                {
                    <div class="no-image">
                        <img src="https://ik.imagekit.io/rqxdk712q/products/733b35b7-b949-4a3c-9e64-1de44004c88e_NoImageLogo" class="main-image" alt="No image" />
                    </div>
                }
            </div>

            <div class="info-card">

                <div class="price-availability">
                    @if (Model.Price.HasValue && Model.Price > 0)
                    {
                        <div class="product-price">@Model.Price.Value.ToString("N2") ₾</div>
                    }
                    else
                    {
                        <div class="price-missing">
                            <i class="bi bi-exclamation-triangle"></i> ფასი არაა მითითებული
                        </div>
                    }
                    @if (Model.IsAvailable == true)
                    {
                        <span class="availability-badge in-stock">მარაგშია</span>
                    }
                    else
                    {
                        <span class="availability-badge out-of-stock">არ არის მარაგში</span>
                    }
                </div>

                <div class="specs-section-upgraded">
                    <div class="specs-header">
                        <span class="specs-accent"></span>
                        <span class="specs-title">
                            <svg width="26" height="26" viewBox="0 0 24 24" fill="none">
                                <path d="M7 20v-9M7 9V4M7 9h10M17 9v11M17 9V4M17 9H7" stroke="#6366f1" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                            </svg>
                            ტექნიკური მახასიათებლები
                        </span>
                    </div>
                    <div class="specs-grid-upgraded">
                        @if (Model.Category != null)
                        {
                            <div class="spec-card">
                                <div class="spec-icon"><i class="bi bi-layers"></i></div>
                                <div>
                                    <div class="spec-label-upgraded">კატეგორია</div>
                                    <div class="spec-value-upgraded">@Model.Category.Name</div>
                                </div>
                            </div>
                        }
                        @if (Model.Brand != null)
                        {
                            <div class="spec-card">
                                <div class="spec-icon"><i class="bi bi-award"></i></div>
                                <div>
                                    <div class="spec-label-upgraded">ბრენდი</div>
                                    <div class="spec-value-upgraded">@Model.Brand.Name</div>
                                </div>
                            </div>
                        }
                        @if (Model.Type != null)
                        {
                            <div class="spec-card">
                                <div class="spec-icon"><i class="bi bi-box"></i></div>
                                <div>
                                    <div class="spec-label-upgraded">ტიპი</div>
                                    <div class="spec-value-upgraded">@getGeorgianType(Model.Type.Value)</div>
                                </div>
                            </div>
                        }
                        @if (!string.IsNullOrWhiteSpace(Model.Color))
                        {
                            <div class="spec-card">
                                <div class="spec-icon"><i class="bi bi-palette"></i></div>
                                <div>
                                    <div class="spec-label-upgraded">ფერი</div>
                                    <div class="spec-value-upgraded">@Model.Color</div>
                                </div>
                            </div>
                        }
                        @if (Model.Length.HasValue)
                        {
                            <div class="spec-card">
                                <div class="spec-icon"><i class="bi bi-arrows-expand"></i></div>
                                <div>
                                    <div class="spec-label-upgraded">სიგრძე</div>
                                    <div class="spec-value-upgraded">@Model.Length მ</div>
                                </div>
                            </div>
                        }
                        @if (Model.Width.HasValue)
                        {
                            <div class="spec-card">
                                <div class="spec-icon"><i class="bi bi-arrows-expand"></i></div>
                                <div>
                                    <div class="spec-label-upgraded">სიგანე</div>
                                    <div class="spec-value-upgraded">@Model.Width მ</div>
                                </div>
                            </div>
                        }
                        @if (Model.Height.HasValue)
                        {
                            <div class="spec-card">
                                <div class="spec-icon"><i class="bi bi-arrows-expand-vertical"></i></div>
                                <div>
                                    <div class="spec-label-upgraded">სიმაღლე</div>
                                    <div class="spec-value-upgraded">@Model.Height მ</div>
                                </div>
                            </div>
                        }
                        @if (Model.ProducerCountry != null)
                        {
                            <div class="spec-card">
                                <div class="spec-icon"><i class="bi bi-geo-alt"></i></div>
                                <div>
                                    <div class="spec-label-upgraded">მწარმოებელი ქვეყანა</div>
                                    <div class="spec-value-upgraded">@Model.ProducerCountry.Name</div>
                                </div>
                            </div>
                        }
                    </div>
                </div>

                <div class="actions-section-upgraded">
                    <a href="javascript:history.back()" class="btn-back-upgraded">
                        <svg width="22" height="22" viewBox="0 0 20 20" fill="none">
                            <path d="M7.707 16.707a1 1 0 0 1-1.414 0l-6-6a1 1 0 0 1 0-1.414l6-6a1 1 0 0 1 1.414 1.414L3.414 9H15a1 1 0 1 1 0 2H3.414l4.293 4.293a1 1 0 0 1 0 1.414z" fill="currentColor" />
                        </svg>
                        უკან დაბრუნება
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

@if (Model.Images != null && Model.Images.Any())
{
    <div id="modalBackdrop" class="modal-backdrop" onclick="closeModal(event)">
        <div class="modal-content" onclick="event.stopPropagation()">
            <button class="modal-close" onclick="closeModal(event)">&times;</button>
            @if (Model.Images.Count > 1)
            {
                <button class="modal-arrow left" onclick="prevImage(event)">‹</button>
                <button class="modal-arrow right" onclick="nextImage(event)">›</button>
            }
            <img id="modalMainImage" class="modal-main-image" src="" alt="Product image" />
            <div id="zoomLens" class="zoom-lens"></div>
            <div class="modal-thumbnails" id="modalThumbnails"></div>
        </div>
    </div>
}

@section Scripts {
    <script>

        const modalMainImage = document.getElementById('modalMainImage');
        const zoomLens = document.getElementById('zoomLens');
        const isNoImage = modalMainImage?.closest('.no-image');

        if (modalMainImage && zoomLens && !isNoImage) {
            modalMainImage.addEventListener('mousemove', moveLens);
            modalMainImage.addEventListener('mouseenter', showLens);
            modalMainImage.addEventListener('mouseleave', hideLens);


            function showLens(e) {
                zoomLens.style.display = 'block';
                zoomLens.style.backgroundImage = `url('${modalMainImage.src}')`;
            }

            function hideLens(e) {
                zoomLens.style.display = 'none';
            }

            function moveLens(e) {
                const rect = modalMainImage.getBoundingClientRect();
                const x = e.clientX - rect.left;
                const y = e.clientY - rect.top;

                const lensSize = zoomLens.offsetWidth / 2;
                let lensX = x - lensSize;
                let lensY = y - lensSize;

                lensX = Math.max(0, Math.min(lensX, rect.width - zoomLens.offsetWidth));
                lensY = Math.max(0, Math.min(lensY, rect.height - zoomLens.offsetHeight));

                zoomLens.style.left = (modalMainImage.offsetLeft + lensX) + 'px';
                zoomLens.style.top = (modalMainImage.offsetTop + lensY) + 'px';

                const percentX = x / rect.width * 100;
                const percentY = y / rect.height * 100;
                zoomLens.style.backgroundPosition = `${percentX}% ${percentY}%`;
                zoomLens.style.backgroundSize = `${rect.width * 2}px ${rect.height * 2}px`;
            }
        }

        document.addEventListener('DOMContentLoaded', function () {
            const carousel = document.getElementById('similarProductsCarouselFlex');
            if (!carousel) return;
            const cards = carousel.querySelectorAll('.similar-product-card');
            if (cards.length <= 1) return;

            const carouselOuter = carousel.parentElement;
            const cardWidth = cards[0].offsetWidth + 32; 
            const visibleCount = Math.floor(carouselOuter.offsetWidth / cardWidth);

            let currentIndex = 0;

            function scrollCarousel() {
                if (currentIndex >= cards.length - visibleCount) {
                    currentIndex = 0;
                } else {
                    currentIndex++;
                }
                carousel.style.transform = `translateX(-${currentIndex * cardWidth}px)`;
            }

            setInterval(scrollCarousel, 3000);
        });

        @if (Model.Images != null && Model.Images.Any())
        {
            @:var images = [
            foreach (var img in Model.Images)
            {
                @:{ url: '@img.FileUrl', alt: '@img.FileName' },
            }
            @:];
            @:var currentIndex = 0;
            @:var selectedIndex = 0;
        }

        function showTooltip() {
            var tooltip = document.getElementById('mainImageTooltip');
            if (tooltip) tooltip.style.opacity = '1';
        }
        function hideTooltip() {
            var tooltip = document.getElementById('mainImageTooltip');
            if (tooltip) tooltip.style.opacity = '0';
        }

        function changeImage(index) {
            const mainImg = document.getElementById('mainImage');
            const thumbnails = document.querySelectorAll('.thumbnail');

            if (mainImg && images && images[index]) {
                mainImg.src = images[index].url;
                thumbnails.forEach((thumb, i) => {
                    thumb.classList.toggle('active', i === index);
                });
                selectedIndex = index; 
            }
        }

        function openModal(idx) {
            currentIndex = idx;
            updateModal();
            document.getElementById('modalBackdrop').classList.add('active');
            document.body.style.overflow = 'hidden';
        }

        function closeModal(e) {
            if (e) e.preventDefault();
            document.getElementById('modalBackdrop').classList.remove('active');
            document.body.style.overflow = '';
        }

        function updateModal() {
            const mainImg = document.getElementById('modalMainImage');
            const thumbs = document.getElementById('modalThumbnails');

            if (!mainImg || !thumbs || !images) return;

                mainImg.src = images[currentIndex].url;
                mainImg.alt = images[currentIndex].alt;
                if (zoomLens) {
                    zoomLens.style.backgroundImage = `url('${images[currentIndex].url}')`;
                }

            thumbs.innerHTML = '';
            images.forEach((img, i) => {
                const thumb = document.createElement('img');
                thumb.src = img.url;
                thumb.alt = img.alt;
                thumb.className = 'modal-thumbnail' + (i === currentIndex ? ' selected' : '');
                thumb.onclick = () => { currentIndex = i; updateModal(); };
                thumbs.appendChild(thumb);
            });
        }

        function prevImage(e) {
            if (e) e.stopPropagation();
            if (!images) return;
            currentIndex = (currentIndex - 1 + images.length) % images.length;
            updateModal();
        }

        function nextImage(e) {
            if (e) e.stopPropagation();
            if (!images) return;
            currentIndex = (currentIndex + 1) % images.length;
            updateModal();
        }

        document.addEventListener('keydown', (e) => {
            const modal = document.getElementById('modalBackdrop');
            if (!modal || !modal.classList.contains('active')) return;

            if (e.key === 'Escape') closeModal(e);
            if (e.key === 'ArrowLeft') prevImage(e);
            if (e.key === 'ArrowRight') nextImage(e);
        });

        document.addEventListener('DOMContentLoaded', function () {
            if (images && images.length > 0) {
                document.getElementById('mainImage').src = images[0].url;
                selectedIndex = 0;
            }
        });
    </script>
}

@{
    var similarProducts = (ViewBag.SimilarProducts as List<SimilarProductDto>)?.Where(x => x.Id != Model.Id).ToList();
}
<div class="similar-products-section" style="margin-top:3rem;">
    <div style="display:flex;align-items:center;gap:1rem;margin-bottom:1.5rem;">
        <svg width="28" height="28" fill="none" viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="10" fill="#6366f1" fill-opacity="0.12" />
            <path d="M8 12h8M12 8v8" stroke="#6366f1" stroke-width="2" stroke-linecap="round" />
        </svg>
        <h2 style="margin:0;font-size:1.5rem;font-weight:800;color:#6366f1;letter-spacing:-0.5px;">
            მსგავსი პროდუქტები
        </h2>
        <div style="flex:1;height:2px;background:linear-gradient(90deg,#6366f1 0%,#ec4899 100%);border-radius:2px;"></div>
    </div>
    @if (similarProducts != null && similarProducts.Any())
    {
        <div class="similar-products-carousel-outer">
            <div class="similar-products-carousel" id="similarProductsCarouselFlex">
                @foreach (var product in similarProducts)
                {
                    <div class="similar-product-card">
                        @if (string.IsNullOrWhiteSpace(product.ImageUrl))
                        {
                            <div class="no-image">
                                <img src="https://ik.imagekit.io/rqxdk712q/products/733b35b7-b949-4a3c-9e64-1de44004c88e_NoImageLogo" class="main-image" alt="No image" />
                            </div>
                        }
                        else
                        {
                            <img src="@product.ImageUrl" class="similar-product-image" alt="@product.Title" />
                        }
                        <h5>@product.Title</h5>
                        @if (product.Price != null && product.Price > 0)
                        {
                            <div class="price-tag" style="color:#6366f1;font-weight:700;font-size:1.05rem;">
                                @product.Price <span style="font-size:1.1rem;">₾</span>
                            </div>
                        }
                        else
                        {
                            <div class="price-missing"> <i class="bi bi-exclamation-circle"></i> ფასი არაა მითითებული </div>
                        }
                        <a href="@Url.Action("ProductDetails", "Home", new { id = product.Id })" class="btn btn-primary" style="background:linear-gradient(90deg,#6366f1 0%,#ec4899 100%);border:none;">
                            ნახვა
                        </a>
                    </div>
                }
            </div>
        </div>
    }
    else
    {
        <div style="color:#64748b;font-size:1.1rem;">მსგავსი პროდუქტები ვერ მოიძებნა.</div>
    }
</div>