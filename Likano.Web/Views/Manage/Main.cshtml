@{
    ViewData["Title"] = "სამართავი პანელი";
}
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

<div class="dashboard-header mb-5 text-center pt-5">
    <div class="brand-title mb-4">
        <h1 class="brand-logo fw-bold text-gradient mb-1">
            LIKANO<span class="text-primary">.GE</span>
        </h1>
    </div>
    <h2 class="fw-bold mb-3 text-gradient fs-3">
        <i class="fas fa-sliders-h me-2"></i>სამართავი პანელი
    </h2>
    <p class="text-muted fs-5">მართეთ კატეგორიები და პროდუქცია მარტივად და სწრაფად</p>
</div>

<div class="row g-4 justify-content-center">
   
    <div class="col-12 col-md-6 col-lg-5">
        <a asp-controller="Manage" asp-action="Categories" class="dashboard-link">
            <div class="dashboard-card glass">
                <div class="card-icon bg-purple">
                    <i class="fas fa-sitemap"></i>
                </div>
                <div class="card-content">
                    <h5>კატეგორიები</h5>
                    <p>კატეგორიების ნახვა, ფილტრი და სტატუსის ცვლილება</p>
                </div>
                <div class="card-arrow">
                    <i class="fas fa-arrow-right"></i>
                </div>
            </div>
        </a>
    </div>

    <div class="col-12 col-md-6 col-lg-5">
        <a asp-controller="Manage" asp-action="Products" class="dashboard-link">
            <div class="dashboard-card glass">
                <div class="card-icon bg-blue">
                    <i class="fas fa-box-open"></i>
                </div>
                <div class="card-content">
                    <h5>პროდუქცია</h5>
                    <p>პროდუქტების ნახვა, ფილტრი და სტატუსის ცვლილება</p>
                </div>
                <div class="card-arrow">
                    <i class="fas fa-arrow-right"></i>
                </div>
            </div>
        </a>
    </div>
</div>

<style>
    body {
        font-family: 'Inter', sans-serif;
        background: radial-gradient(circle at top, #f4f6fb, #eef1f7);
    }

    .text-gradient {
        background: linear-gradient(135deg, #667eea, #764ba2);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .brand-logo {
        font-size: 3.2rem; 
        letter-spacing: -1px;
        margin: 0;
    }

    .dashboard-link {
        text-decoration: none;
    }

    .dashboard-card {
        position: relative;
        display: flex;
        align-items: center;
        gap: 20px;
        padding: 28px;
        border-radius: 22px;
        transition: all 0.35s ease;
        cursor: pointer;
    }

    .glass {
        background: rgba(255, 255, 255, 0.75);
        backdrop-filter: blur(14px);
        box-shadow: 0 20px 40px rgba(0,0,0,0.08);
        border: 1px solid rgba(255,255,255,0.5);
    }

    .dashboard-card:hover {
        transform: translateY(-6px) scale(1.01);
        box-shadow: 0 30px 60px rgba(102,126,234,0.25);
    }

    .card-icon {
        width: 64px;
        height: 64px;
        min-width: 64px;
        border-radius: 18px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #fff;
        font-size: 26px;
        box-shadow: 0 10px 24px rgba(0,0,0,0.25);
    }

    .bg-purple {
        background: linear-gradient(135deg, #667eea, #764ba2);
    }

    .bg-blue {
        background: linear-gradient(135deg, #43cea2, #185a9d);
    }

    .card-content h5 {
        margin: 0 0 6px;
        font-weight: 600;
        color: #2d2f36;
    }

    .card-content p {
        margin: 0;
        font-size: 14px;
        color: #6c757d;
    }

    .card-arrow {
        margin-left: auto;
        font-size: 20px;
        color: #667eea;
        transition: transform 0.3s ease;
    }

    .dashboard-card:hover .card-arrow {
        transform: translateX(6px);
    }

    @@media (max-width: 768px) {
        .brand-logo

    {
        font-size: 2.5rem;
    }

    .dashboard-card {
        padding: 20px;
    }

    .card-icon {
        width: 54px;
        height: 54px;
        font-size: 22px;
    }

    }
</style>

@section Scripts {
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
}